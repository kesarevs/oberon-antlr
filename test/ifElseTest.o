MODULE FigureList;
VAR
    MyINT : INTEGER;
BEGIN
    IF 1 THEN
        MyINT := 1;
    END ;

    IF 0 THEN
        MyINT := 0;
    END ;

    IF 0 THEN
        MyINT := 0;
    ELSIF 0 THEN
        MyINT := 0;
    ELSIF 0 THEN
        MyINT := 0;
    ELSIF 1 THEN
        MyINT := 1;   
    END ;
    IF 0 THEN
        MyINT := 0;
    ELSE
        MyINT := 1;   
    END ;
    IF 0 THEN
        MyINT := 0;
    ELSIF 0 THEN
        MyINT := 0;
    ELSIF 0 THEN
        MyINT := 0; 
    ELSE
        MyINT := 1;   
    END ;
END FigureList.